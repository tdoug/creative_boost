# ============================================
# Cloud Provider Selection
# ============================================
# Choose: aws | azure | gcp
CLOUD_PROVIDER=aws
CLOUD_REGION=us-east-1

# ============================================
# AWS Configuration (Bedrock + S3)
# ============================================
# Used when CLOUD_PROVIDER=aws
AWS_REGION=us-east-1
# AWS credentials will be read from your local AWS CLI configuration
# Make sure you have run: aws configure
AWS_S3_BUCKET=local
# Use 'local' for filesystem storage (no S3 required)
# Or set to actual S3 bucket name: creative-boost-assets

# Bedrock Models
# Note: Use the versioned model ID format (with :0 suffix)
# Available models:
#   - amazon.titan-image-generator-v2:0 (Titan Image v2 - on-demand) ⭐ RECOMMENDED
#   - amazon.nova-canvas-v1:0 (Nova Canvas - on-demand, better quality)
#   - stability.stable-diffusion-xl-v1:0 (SDXL 1.0 - deprecated/end-of-life)
# Text/LLM: Use US cross-region inference profile for Claude
BEDROCK_MODEL_ID=amazon.titan-image-generator-v2:0
BEDROCK_LLM_MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0

# ============================================
# Azure Configuration (OpenAI + Blob Storage)
# ============================================
# Used when CLOUD_PROVIDER=azure
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_API_KEY=your-api-key
AZURE_OPENAI_DEPLOYMENT_NAME=dall-e-3
AZURE_OPENAI_API_VERSION=2024-02-01
AZURE_STORAGE_ACCOUNT=creativeboost
AZURE_STORAGE_CONTAINER=local
# Use 'local' for filesystem storage (no Azure Blob required)
# Or set to actual container name: campaign-assets

# ============================================
# GCP Configuration (Vertex AI + Cloud Storage)
# ============================================
# Used when CLOUD_PROVIDER=gcp
GCP_PROJECT_ID=your-project-id
GCP_LOCATION=us-central1
GCP_BUCKET=local
# Use 'local' for filesystem storage (no Cloud Storage required)
# Or set to actual bucket name: creative-boost-assets
VERTEX_AI_MODEL=imagegeneration@006
# Credentials via: GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ============================================
# Storage Configuration (for local mode)
# ============================================
STORAGE_PATH=./output

# ============================================
# Optional Features
# ============================================
ENABLE_BRAND_CHECK=false
ENABLE_LEGAL_CHECK=false
LOG_LEVEL=info

# ============================================
# API Configuration
# ============================================
PORT=3000

# ============================================
# Analytics Configuration
# ============================================
# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
GA_MEASUREMENT_ID=G-XXXXXXXXXX
